export interface UserInfoType {
    _id: string;
    email: string;
    name: string;
    createdAt: string;
    updatedAt: string;
    password: string;
    refreshToken: string;
    role: number;
  }